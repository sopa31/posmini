# คู่มือติดตั้งระบบ POS (Google Sheet เป็นฐานข้อมูล)

ระบบนี้มี 2 ส่วน:
- **index.html** — เว็บแอปหน้าร้าน (ใช้งานบนมือถือ)
- **Code.gs** — สคริปต์ฝั่ง Google Sheet ที่ทำหน้าที่เป็นฐานข้อมูล + API

---

## ขั้นตอนที่ 1: สร้าง Google Sheet และวางสคริปต์

1. เปิด [Google Sheets](https://sheets.google.com) แล้วสร้างไฟล์ใหม่ ตั้งชื่อ เช่น `POS Database`
2. เมนู **ส่วนขยาย (Extensions) → Apps Script**
3. ลบโค้ดเดิมทั้งหมดในไฟล์ `Code.gs` แล้ววางโค้ดจากไฟล์ `Code.gs` ที่แนบมาให้แทน
4. แก้บรรทัดนี้ให้เป็นรหัสลับของคุณเอง (ใช้ตัวอักษร/ตัวเลขยาว ๆ คาดเดายาก):
   ```js
   const API_TOKEN = 'change-this-secret-token';
   ```
5. กด **บันทึก** (ไอคอนแผ่นดิสก์ หรือ Ctrl+S)

## ขั้นตอนที่ 2: Deploy เป็น Web App

1. มุมขวาบนของ Apps Script กด **Deploy → New deployment**
2. ที่ "Select type" กดไอคอนเฟือง แล้วเลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** Me (บัญชีของคุณ)
   - **Who has access:** Anyone
4. กด **Deploy** ระบบจะขอสิทธิ์การเข้าถึง Google Sheet ของคุณ — กด **Authorize** แล้วอนุญาตทั้งหมด (เป็นสคริปต์ของคุณเอง ปลอดภัย)
5. คัดลอก **Web app URL** ที่ได้ (ขึ้นต้นด้วย `https://script.google.com/macros/s/.../exec`) เก็บไว้ใช้ในขั้นตอนถัดไป

> **หมายเหตุ:** ทุกครั้งที่แก้โค้ดใน Code.gs แล้วต้องการให้มีผล ต้องกด Deploy → Manage deployments → แก้ไข (ไอคอนดินสอ) → เลือก Version: New → Deploy อีกครั้ง

## ขั้นตอนที่ 3: เปิดใช้งานเว็บแอป

1. เปิดไฟล์ `index.html` (ดับเบิลคลิกเปิดในเบราว์เซอร์ หรือแนะนำให้อัปโหลดขึ้นโฮสติ้งฟรี เช่น GitHub Pages / Netlify / Firebase Hosting เพื่อให้ใช้งานผ่าน URL แบบ https ได้สะดวกและแชร์ให้พนักงานเปิดจากมือถือได้)
2. แอปจะพาไปหน้า **ตั้งค่า** โดยอัตโนมัติในการเปิดครั้งแรก
3. กรอก:
   - **Web App URL** — URL ที่คัดลอกจากขั้นตอนที่ 2
   - **Token / รหัสลับ API** — ค่าเดียวกับ `API_TOKEN` ใน Code.gs
4. กด **ทดสอบการเชื่อมต่อ** ควรขึ้น "✔ เชื่อมต่อสำเร็จ"
5. กรอกชื่อร้าน ที่อยู่ อัตราภาษี และอัปโหลดโลโก้ร้าน (จะไปแสดงบนหัวใบเสร็จ) แล้วกด **บันทึกการตั้งค่า**

## ขั้นตอนที่ 4: เข้าสู่ระบบ

- ระบบจะสร้างผู้ใช้เริ่มต้นให้อัตโนมัติ: **admin / PIN: 1234**
- แนะนำให้เข้าหน้า **ผู้ใช้** เพื่อเปลี่ยน PIN และเพิ่มพนักงานคนอื่น ๆ ทันที

## การใช้งานประจำวัน

- **ขาย**: แตะสินค้าเพื่อใส่ตะกร้า → แตะแถบตะกร้าด้านล่าง → ชำระเงิน → ยืนยันการขาย ระบบจะเปิดหน้าต่างพิมพ์ใบเสร็จให้อัตโนมัติ (เลือกเครื่องพิมพ์ใบเสร็จ หรือ Save as PDF ก็ได้)
- **รายงาน**: ดูยอดขายวันนี้ / 7 วันล่าสุด / สินค้าขายดี
- **สินค้า / ผู้ใช้**: เพิ่ม แก้ไข ลบ ได้จากหน้าเมนูด้านล่าง

## คำถามที่พบบ่อย

**ใช้งานหลายเครื่อง/หลายพนักงานพร้อมกันได้ไหม?**
ได้ ทุกเครื่องที่กรอก Web App URL และ Token เดียวกัน จะอ่าน/เขียนข้อมูลใน Google Sheet แผ่นเดียวกัน

**ข้อมูลปลอดภัยแค่ไหน?**
ข้อมูลทั้งหมดอยู่ใน Google Sheet ของคุณเอง ไม่มีเซิร์ฟเวอร์อื่นเก็บข้อมูล การเรียก API ทุกครั้งต้องมี Token ที่ถูกต้องเท่านั้นจึงจะเข้าถึงข้อมูลได้ อย่างไรก็ตามลิงก์ Web App เป็นแบบ "Anyone" (ใครมี Token ก็เรียกได้) จึงควรตั้ง Token ให้คาดเดายากและไม่เผยแพร่ต่อสาธารณะ

**ทำไมพิมพ์ใบเสร็จแล้วหน้าตาไม่ตรงกับกระดาษความร้อน (thermal printer)?**
ในหน้าต่างพิมพ์ของเบราว์เซอร์ ให้เลือกขนาดกระดาษให้ตรงกับเครื่องพิมพ์ (เช่น 58mm/80mm) หากใช้เครื่องพิมพ์ใบเสร็จ Bluetooth/USB บนมือถือ แนะนำเปิดผ่าน Chrome และเลือกเครื่องพิมพ์นั้นจากเมนูพิมพ์

**ถ้าลืม PIN ผู้ดูแลระบบ (admin) ทำอย่างไร?**
เปิด Google Sheet ที่เชื่อมต่อไว้ ไปที่ชีต `Users` แล้วแก้ค่าคอลัมน์ `pin` ของแถว admin ได้โดยตรง
